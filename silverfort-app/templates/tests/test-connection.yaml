apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "silverfort-app.fullname" . }}-test-connection"
  labels:
    {{- include "silverfort-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "silverfort-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
